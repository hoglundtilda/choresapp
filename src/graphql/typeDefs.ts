import {
  // categorySchema,
  // commonSchema,
  // choreSchema,
  // timeRecordSchema,
  userSchema
} from './schemas'

export const typeDefs = [
  // categorySchema,
  // commonSchema,
  // choreSchema,
  // timeRecordSchema,
  userSchema
]
